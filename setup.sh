# run to add chromedriver to the path
echo "This script is for adding chromedriver to the PATH environment variable
echo "Make sure to run this script by using 'source setup.sh' for it to be able to change the PATH variable of your session."
export PATH=$PATH:`pwd`

